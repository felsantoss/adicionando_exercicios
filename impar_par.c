#include <stdio.h>
#include <stdlib.h>

int main ()
{
    int num;

    printf("Digite um numero: ");
    scanf("%d", &num);

    if ( num % 2 == 0 && )
        printf("O numero %d eh par e maior que 10. ", num);

    else   
        printf("O numero eh %d impar e menor que 10. ", num);

    return 0;
}